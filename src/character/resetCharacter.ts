// import { loadCharactersState } from "./loadCharactersState";

export const resetCharacter = async (characterId: string) => {
  throw new Error("TBD");
  // const character = getCharacter(characterId);
  // if (!character) {
  //   return;
  // }
  // const cards = await pngToTavernCards(character.image);
  // await Idbs.put(CharactersDB, characterId, { ...character, card: cards[0] });
  // await loadCharactersState();
};

import { addReturnListener } from "./addReturnListener";
import { addStreamListener } from "./addStreamListener";
import { signalToAbortHandler } from "./signalToAbortHandler";
export const Returns = {
  addReturnListener,
  addStreamListener,
  signalToAbortHandler,
};
